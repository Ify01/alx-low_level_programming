#include "main.h"

/**
 * _isalpha - prints upper case.
 * @c: initialze c
 * Return: return successfully.
 */

int _isalpha(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
