#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - printing the value of positive, negative and zeor with string
 *
 * Return: Always 0 is success
 */

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;

	if 
}
